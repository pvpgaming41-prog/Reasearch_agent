{
  "name": "core-research-agent",
  "version": "0.1.0",
  "description": "Minimal backend: real browser (Playwright) + real filesystem, driven by an LLM tool-calling loop, streamed to a UI over WebSocket.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browser": "playwright install chromium"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "express": "^4.19.2",
    "playwright": "^1.47.0",
    "ws": "^8.18.0",
    "dotenv": "^16.4.5"
  }
}
